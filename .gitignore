# TeX compile artifacts (we keep .tex sources, .pdf outputs, and .svg files)
build/*.log
build/*.aux
build/*.out
build/*.synctex.gz
build/*.fls
build/*.fdb_latexmk

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor junk
.vscode/
.idea/
*.swp
*~
